# Path to the model configuration file used by the demo.
# You can override at runtime via: ant -Dmodel.path=/path/to/model.json run
model.path=${basedir}/app/build/config/model/model.configuration.example.json
# Output CSV path for demo predictions.
output.path=${basedir}/app/build/predictions.csv
